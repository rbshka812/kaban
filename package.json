{
  "name": "cybershoke-live",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time Cybershoke CS2 server browser with player Steam inventory values",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'no build step — static + serverless'",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
